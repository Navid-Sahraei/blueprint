/**
 * Temporary dev flag: signups closed and /app ungated so the app can be
 * exercised without wiring up a live Supabase auth flow first.
 * Flip to true to re-require auth and re-open signup.
 */
export const AUTH_REQUIRED = false;
